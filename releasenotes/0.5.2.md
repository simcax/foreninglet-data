## Release Notes - Version 0.5.2

### Bug Fixes

#### Pandas 3.0 Compatibility
- **Test Suite Fix**: Updated `test_memberlist_has_correct_gender_count` to work with pandas 3.0
  - Fixed `TypeError: initial_value must be str or None, not list` error
  - Added `json.dumps()` conversion for list data before passing to `pd.read_json(StringIO())`
  - Pandas 3.0 enforces stricter type checking for `StringIO` input

### Dependencies Updated

#### Core Dependencies
- **`pandas>=2.1.4`**: Now compatible with pandas 3.0.1
  - Stricter type validation in `pd.read_json()` with `StringIO`
  - All tests passing with pandas 3.x

#### Development Dependencies
- **`black>=25.12.0`**: Updated from 23.7.0 (code formatter)
- **`faker>=24.14.1`**: Updated from 19.2 (test data generation)
- **`matplotlib>=3.10.8`**: Updated (data visualization)
- **`pytest>=9.0.2`**: Updated from 8.0.0 (testing framework)
- **`pytest-cov>=5.0.0`**: Updated from 4.1 (coverage reporting)
- **`python-dotenv>=1.0.1`**: Updated from 1.2.1 (environment management)
- **`typer>=0.12.5`**: Updated from 0.9 (CLI framework)

### Technical Details
- **Python Compatibility**: Maintains support for Python >=3.11
- **Breaking Change Prevention**: Ensures compatibility with both pandas 2.x and 3.x
- **Test Coverage**: All 35 tests passing with updated dependencies

### Migration Notes
- No breaking changes to library API
- Projects using pandas 3.0+ will work seamlessly
- Existing code using pandas 2.x remains fully compatible
